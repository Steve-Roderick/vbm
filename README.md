# vbm.sh - Minimal wrapper for VBoxManage.

`./vbm.sh --help`

Writes:
```
     vm.sh [start|stop] <Virtual Box Name> # Start or stop a vm.
     vm.sh status                          # List running VMS.
     vm.sh ls                              # List available VMS.
     vm.sh [-h|--help]
```
